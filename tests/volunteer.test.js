const { validateCharityName, validateHours, validateDate, validateStarRating } = require("../volunteer.js");

const { JSDOM } = require("jsdom");

test("input validation: charity name has at least 1 character", () => {
    // AAA ("triple-a") pattern
    // arrange
    

    // act
    

    // assert
    // all "expect" invocations must be true for the test to pass
    
});